# Python Environment Settings
PYTHONPATH=.

# Secret key placeholder for future frameworks
SECRET_KEY=STRATEGICAI_SECRET_REPLACE_ME_32CHARS_MIN

# --- PostgreSQL connection (hosted locally via Docker) ---
# These must match docker-compose.yml env for the "db" service.
DB_USER=user
DB_PASSWORD=pass
DB_HOST=localhost
DB_PORT=5432
DB_NAME=appdb

DATABASE_URL=postgresql+psycopg://user:pass@localhost:5432/appdb
# Psycopg driver:
# Leave PSYCOPG_* unset so psycopg-binary is used (avoids libpq issues on macOS).
# If you need pure-python temporarily, export PSYCOPG_IMPL=python in your shell (don’t commit it to .env).

# Debugging mode (set to False in production)
DEBUG=False
ALLOWED_HOSTS=*