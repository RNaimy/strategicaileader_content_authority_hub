# --- Database (local dev) ---
# Point the app & Alembic at your local Postgres. If unset, the app may fall back to SQLite (./data/app.db).
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/appdb
# To use SQLite instead, comment the line above and uncomment the line below.
# DATABASE_URL=sqlite:///./data/app.db

# --- Google Analytics 4 (GA4) OAuth ---
GA4_CLIENT_ID=your-oauth-client-id
GA4_CLIENT_SECRET=your-oauth-client-secret
GA4_REFRESH_TOKEN=your-refresh-token    # do NOT commit real token
# Optional: service-account key path (alternative to OAuth)
# GOOGLE_APPLICATION_CREDENTIALS=./keys/ga4-service.json

# --- GA4 Property IDs (multiple sites supported) ---
GA4_PROPERTY_ID_STRATEGICAI=354557242
GA4_PROPERTY_ID_LIASFLOWERS=123456789

# --- Google Search Console (GSC) OAuth ---
GSC_CLIENT_ID=your-oauth-client-id
GSC_CLIENT_SECRET=your-oauth-client-secret
GSC_REFRESH_TOKEN=your-refresh-token    # do NOT commit real token

# --- App server (local dev) ---
# General runtime knobs for local FastAPI/uvicorn
APP_ENV=development
HOST=127.0.0.1
PORT=8000
LOG_LEVEL=info

# --- CORS ---
# Comma-separated list of origins. Use * only for local dev.
CORS_ALLOW_ORIGINS=*

# --- Graph export ---
# Where the /graph/export endpoint writes when using the Makefile helper
GRAPH_EXPORT_DIR=graph/export

# --- Security ---
# Used for signing sessions/tokens in dev. Replace in prod.
SECRET_KEY=change-me-in-production

# --- Tests (optional) ---
# If set, tests can point to a separate DB. Otherwise they use DATABASE_URL.
TEST_DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/appdb_test

# --- OAuth redirect URIs (optional, docs) ---
# OAUTH_REDIRECT_URI=http://localhost:8000/auth/callback
